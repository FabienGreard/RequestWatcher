const Deployment = require('./Deployment');
const Notification = require('./Notification');
const AverageBox = require('./AverageBox');
const Graph = require('./Graph.js');

module.exports = { Deployment, Notification, AverageBox, Graph };
